
## Authentication setup update

Source: https://console.firebase.google.com/project/sogligim-prod-db270/authentication/providers

The Firebase Authentication Sign-in method page is now open for `sogligim-prod-db270`. The Google provider toggle has been enabled, but Firebase requires a public-facing project name and support email before the Save action becomes available. The public-facing name is currently `project-1098824868523`; the support email selector is still `Not configured`.

The Google provider is enabled and saved. Firebase authorized domains now include `wffwff56-ai.github.io` as a Custom domain, alongside the default Firebase domains and localhost.
